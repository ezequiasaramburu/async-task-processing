# Local API + worker: copy to .env and adjust stage if needed.
# Values align with serverless.yml provider.stage and resource names (tasks-${stage}, task-queue-${stage}).

IS_OFFLINE=true
AWS_REGION=us-east-1
SQS_ENDPOINT=http://localhost:4566

TASK_QUEUE_NAME=task-queue-dev
TASKS_TABLE=tasks-dev
