service: async-task-processor

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1

plugins:
  - serverless-esbuild
  - serverless-dynamodb
  - serverless-offline-sqs
  - serverless-offline

custom:
  esbuild:
    bundle: true
    minify: false
    sourcemap: true
    target: node20
